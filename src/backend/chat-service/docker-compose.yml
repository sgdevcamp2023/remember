version: '3'

services:
  chat-service:
    container_name: chat-service
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
    volumes:
      - logs:/log/info

volumes:
  logs: